# Applying this download

This is a Stage 1 specification overlay for `JennieXLisa/ssr-redesign`, based on main commit `c2c238d325d82188e47d8b83b9d963800754d986`. Extract into an existing checkout without deleting its AGENTS.md, requirements or earlier contract documents. Those previously approved files are retained in GitHub, not redundantly copied into this overlay.

Open `stage-1-indexing/IMPLEMENTATION.md`. It names the authoritative design documents and W01–W15 implementation order. `stage-1-indexing/MANIFEST.json` hashes the stage files in this download. The package contains specification/reference assets, not the finished application.
